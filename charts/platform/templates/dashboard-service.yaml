apiVersion: v1
kind: Service
metadata:
  name: {{ include "platform.dashboardName" . }}
spec:
  selector:
    app: {{ include "platform.dashboardName" . }}
  ports:
    - name: http
      port: 80
      targetPort: 80
