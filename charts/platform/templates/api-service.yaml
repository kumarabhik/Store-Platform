apiVersion: v1
kind: Service
metadata:
  name: {{ include "platform.apiName" . }}
spec:
  selector:
    app: {{ include "platform.apiName" . }}
  ports:
    - name: http
      port: 80
      targetPort: 8000
